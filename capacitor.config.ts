import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.liceo.ditls.laboratorybooking',
  appName: 'Liceo Laboratory Booking System',
  server: {
    url: 'https://ditls-laboratory-booking.vercel.app'
  }
};

export default config;
